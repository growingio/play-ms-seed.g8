# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Healthy check controller

GET        /healthy-check        controller.HealthyCheckController.healthyCheck()

GET        /:name                controller.HelloController.index(name: String)
